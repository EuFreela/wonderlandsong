node_modules
Dist
dist
.vite
coverage
*.tsbuildinfo

# Vite tsc emit next to vite.config.ts (source is vite.config.ts)
vite.config.js
vite.config.d.ts

# Lighthouse / perf audit dumps
lighthouse*
*.report.html
*.report.json

# One-off extraction dumps (PDFs, scrapes, debug) — not part of the app
*.pdf
agm-bundle.js
agm-lyrics.json
agm-verses-debug.json
helena-genesis*
helena-parsed.json
caution-lyrics*
*-debug.json
